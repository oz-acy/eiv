"INSTALL" of EIV

cmake, make して出來上がつた eiv.exe を適當なディレクトリに置いて使用されたい。
make install も可能。當然、デフォルトでは /usr/local/bin にインストールされる。

ビルドするには、以下が必要。
 - libthemis, libpolymnia, liburania
 - libpolymnia のために、zlib, libpng, libjpeg
 - libthemis のために、libiconv
   (EIVでは利用してゐないが、libthemisをビルドするには必要)

アンインストールは eiv.exe を削除するだけ。
